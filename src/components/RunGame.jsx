import React, { Component } from "react";
import { observer, inject } from "mobx-react";

@observer
class RunGame extends Component {
  render() {
    return <div>run game</div>;
  }
}
export default RunGame;
